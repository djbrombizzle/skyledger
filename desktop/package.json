{
  "name": "skyledger-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "Electron shell that runs Skyledger with direct SimConnect access (no bridge process).",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}
